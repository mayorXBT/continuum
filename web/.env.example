# Contract addresses on Monad testnet — fill in after plan Task 8 (deployment),
# then copy to .env.local. Until set, the app runs in pre-deploy mode.
NEXT_PUBLIC_APASS=
NEXT_PUBLIC_STMON=
NEXT_PUBLIC_VAULT=
NEXT_PUBLIC_QUEUE=
